<x-public-shell :title="$content->meta_title ?: $content->title" :description="$content->meta_description ?: $content->excerpt">
    <article><header class="relative overflow-hidden bg-emerald-950 py-20 text-white"><div class="absolute inset-0 opacity-10" style="background-image:radial-gradient(circle,#fff 1px,transparent 1px);background-size:27px 27px"></div><div class="relative mx-auto max-w-4xl px-4 text-center sm:px-6"><a href="{{ route($backRoute) }}" class="text-xs font-black text-emerald-300">{{ $section }} / عودة</a><h1 class="mt-5 text-3xl font-black leading-snug sm:text-5xl">{{ $content->title }}</h1><p class="mt-5 text-sm text-emerald-100/60">{{ $content->published_at?->translatedFormat('j F Y') }}</p></div></header><div class="mx-auto max-w-4xl px-4 py-16 sm:px-6">@if($content->featuredMedia)<img class="mb-10 aspect-[16/8] w-full rounded-[2.5rem] object-cover shadow-2xl shadow-emerald-950/10" src="{{ $content->featuredMedia->url }}" alt="{{ $content->featuredMedia->alt_text ?: $content->title }}">@endif@if($content->excerpt)<p class="mb-8 border-r-4 border-emerald-500 pr-5 text-xl font-bold leading-9 text-emerald-950">{{ $content->excerpt }}</p>@endif<div class="whitespace-pre-line text-base leading-9 text-slate-600">{{ $content->body }}</div><div class="mt-12 border-t border-slate-100 pt-8"><a href="{{ route($backRoute) }}" class="btn-secondary">العودة إلى {{ $section }}</a></div></div></article>
</x-public-shell>
